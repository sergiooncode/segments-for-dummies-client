# segments-for-dummies-client
Client app of segment-for-dummies API written in Angular and Flask.

To install the app do:
- Go to the top directory `test_app`.
- Run `env/bin/pip install -r requirements`.
